import 'package:flutter/material.dart';

class AppThemeDark {
  AppThemeDark._();

  static const appbarBackgroundColor = Color.fromARGB(255, 32, 44, 51);
  static const appbarTitleColor = Color(0xFF8696a0);
  static const appbarLabelSelectedColor = Color(0xFF00a884);
  static const appbarLabelUnSelectedColor = Color(0xFF8696a0);
}