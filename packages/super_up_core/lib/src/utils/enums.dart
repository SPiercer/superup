enum SStorageKeys {
  myProfile,
  accessToken,
  isLogin,
  appTheme,
  appLang,
  clintVersion,
  adminAccessPassword,
}

enum RegisterStatus {
  accepted,
  pending,
  notAccepted,
}

enum RegisterMethod { email, phone, apple, google, facebook }
